package nart.simpleanki.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 type scale; customized later if the iOS app's type ramp needs mirroring.
val AzriTypography = Typography()
